#include <stdio.h>

// one hex digit = 4 bits (nibble)
// 4 hex digits = 16 bits = 2 bytes

int main() {
    int N = 0x0120;                                 // hexa decimal value
    int input;                                      // 16-bit key, inputed as hexadecimal value

    printf("Type a hexadecimal key (0xABCD): \n");
    scanf("%d", &input);







}
